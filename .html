<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Soumyajit Das — AI/ML &amp; Cybersecurity</title>
<meta name="description" content="Portfolio of Soumyajit Das — AI/ML and Cybersecurity intern, BCA student, Python developer based in Diamond Harbour, India.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=JetBrains+Mono:wght@400;500;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#0b1220;
    --panel:#131c2e;
    --panel-2:#0e1728;
    --term:#0a1210;
    --amber:#f5a623;
    --amber-dim:#8a6320;
    --cyan:#4fd1c5;
    --cyan-dim:#2c6b64;
    --paper:#f4f2ed;
    --slate:#8b97ac;
    --line:rgba(244,242,237,0.16);
    --line-strong:rgba(244,242,237,0.34);
    --radius:6px;
  }
  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  @media (prefers-reduced-motion: reduce){ html{scroll-behavior:auto;} }
  body{
    margin:0;
    background:var(--ink);
    color:var(--paper);
    font-family:'Inter',system-ui,sans-serif;
    font-weight:500;
    line-height:1.55;
    -webkit-font-smoothing:antialiased;
  }
  body::before{
    content:"";
    position:fixed; inset:0;
    background-image:radial-gradient(rgba(244,242,237,0.05) 1px, transparent 1px);
    background-size:26px 26px;
    pointer-events:none;
    z-index:0;
    mask-image:radial-gradient(circle at 50% 0%, black, transparent 75%);
  }
  a{color:inherit;}
  h1,h2,h3{font-family:'Space Grotesk',sans-serif; font-weight:700; margin:0; letter-spacing:-0.02em;}
  .mono{font-family:'JetBrains Mono',monospace;}
  .wrap{max-width:1080px; margin:0 auto; padding:0 24px; position:relative; z-index:1;}
  .eyebrow{
    font-family:'JetBrains Mono',monospace;
    font-size:12.5px;
    font-weight:700;
    letter-spacing:0.14em;
    text-transform:uppercase;
    background:var(--amber);
    color:#1a1204;
    display:inline-flex; align-items:center; gap:8px;
    margin-bottom:18px;
    padding:6px 12px;
    border-radius:3px;
  }
  .eyebrow.cy{background:var(--cyan); color:#062421;}

  :focus-visible{outline:3px solid var(--amber); outline-offset:3px; border-radius:2px;}

  /* ---------- HEADER ---------- */
  header{
    position:sticky; top:0; z-index:50;
    background:rgba(11,18,32,0.92);
    backdrop-filter:blur(10px);
    border-bottom:2px solid var(--line-strong);
  }
  .navbar{
    max-width:1080px; margin:0 auto; padding:18px 24px;
    display:flex; align-items:center; justify-content:space-between;
  }
  .logo{
    font-family:'JetBrains Mono',monospace; font-weight:700; font-size:17px; letter-spacing:0.02em;
    display:flex; align-items:center; gap:10px; text-decoration:none;
  }
  .logo-box{
    width:34px;height:34px;border:2px solid var(--amber); color:var(--amber); background:rgba(245,166,35,0.08);
    display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:700;border-radius:4px;
  }
  nav.links{display:flex; gap:30px; font-family:'JetBrains Mono',monospace; font-size:13px; font-weight:700; letter-spacing:0.08em; text-transform:uppercase;}
  nav.links a{text-decoration:none; color:var(--slate); position:relative; padding:4px 0;}
  nav.links a:hover{color:var(--paper);}
  .navbar .actions{display:flex; align-items:center; gap:14px;}
  .btn{
    font-family:'JetBrains Mono',monospace; font-size:13px; font-weight:700; letter-spacing:0.06em; text-transform:uppercase;
    padding:12px 20px; border-radius:4px; text-decoration:none; display:inline-flex; align-items:center; gap:8px;
    border:2px solid var(--line-strong); transition:border-color .15s, transform .15s, background .15s;
    white-space:nowrap;
  }
  .btn:hover{transform:translateY(-2px); border-color:var(--paper);}
  .btn-amber{background:var(--amber); color:#1a1204; border-color:var(--amber); font-weight:700;}
  .btn-amber:hover{background:#ffb741; border-color:#ffb741;}
  .btn-ghost{color:var(--paper); border-width:2px;}
  .btn-lg{padding:16px 28px; font-size:14px;}
  #menuToggle{display:none; background:none; border:2px solid var(--line-strong); color:var(--paper); border-radius:4px; padding:8px 10px; font-family:'JetBrains Mono',monospace; font-weight:700;}

  /* ---------- HERO / BOARD ---------- */
  .hero{padding:56px 0 40px;}
  .board{
    border:2px solid var(--line-strong);
    border-radius:var(--radius);
    background:linear-gradient(180deg, var(--panel), var(--panel-2));
    overflow:hidden;
    box-shadow:0 30px 60px -30px rgba(0,0,0,0.7);
  }
  .board-head{
    display:flex; justify-content:space-between; align-items:center;
    padding:14px 20px; border-bottom:2px solid var(--line);
    font-family:'JetBrains Mono',monospace; font-size:12px; font-weight:700; letter-spacing:0.14em; color:var(--slate);
    text-transform:uppercase;
  }
  .board-head .dot{width:9px;height:9px;border-radius:50%; background:var(--amber); display:inline-block; margin-right:8px; animation:pulse 2.2s infinite;}
  @keyframes pulse{0%,100%{opacity:1;}50%{opacity:.35;}}
  @media (prefers-reduced-motion: reduce){ .board-head .dot{animation:none;} }

  .board-row{
    display:grid; grid-template-columns:90px 1fr 1fr 150px;
    gap:12px; padding:18px 20px; border-bottom:2px solid var(--line);
    font-family:'JetBrains Mono',monospace; font-size:14px; font-weight:700; align-items:center;
  }
  .board-row:last-child{border-bottom:none;}
  .board-row .code{color:var(--amber); font-weight:700;}
  .board-row .status{
    justify-self:end; font-size:11.5px; font-weight:700; letter-spacing:0.08em; padding:5px 12px; border-radius:20px;
    border:2px solid currentColor;
  }
  .status.ontime{color:var(--cyan);}
  .status.landed{color:var(--amber);}
  .status.board{color:var(--paper);}
  .board-row .role{color:var(--paper);}
  .board-row .org{color:var(--slate); font-weight:500;}

  .hero-name{padding:44px 20px 36px;}
  .flap-line{
    font-family:'Space Grotesk',sans-serif; font-weight:700;
    font-size:clamp(44px, 10vw, 92px); line-height:0.96; letter-spacing:-0.03em;
    display:flex; flex-wrap:wrap;
    text-shadow:4px 4px 0 rgba(245,166,35,0.4);
  }
  .flap-cell{
    display:inline-block; min-width:0.58em; text-align:center;
    color:var(--paper);
  }
  .flap-cell.flip{animation:flip .12s ease;}
  @keyframes flip{
    0%{transform:rotateX(0deg); opacity:1;}
    45%{transform:rotateX(85deg); opacity:.5;}
    55%{transform:rotateX(85deg); opacity:.5;}
    100%{transform:rotateX(0deg); opacity:1;}
  }
  .tagline{
    margin-top:20px; font-family:'JetBrains Mono',monospace; font-size:18px; font-weight:700;
    color:var(--slate); letter-spacing:-0.01em;
  }
  .tagline .hl{color:var(--amber);}
  .tagline .hl2{color:var(--cyan);}
  .hero-copy{max-width:580px; margin-top:20px; color:var(--slate); font-size:16.5px; font-weight:500;}
  .hero-ctas{display:flex; flex-wrap:wrap; gap:14px; margin-top:32px;}

  /* ---------- SECTIONS ---------- */
  section{padding:84px 0;}
  section h2{font-size:clamp(30px,5.5vw,46px); margin-bottom:12px; text-transform:uppercase;}
  .section-sub{color:var(--slate); max-width:600px; margin-bottom:40px; font-size:15.5px; font-weight:500;}
  .divider{height:2px; background:var(--line-strong); border:none; margin:0;}

  /* about */
  #about p{max-width:680px; color:#cdd3de; font-size:16.5px; font-weight:500;}
  #about strong{color:var(--paper); font-weight:700;}

  /* work cards */
  .ticket-grid{display:grid; grid-template-columns:1fr 1fr; gap:22px;}
  .ticket{
    background:var(--panel); border:2px solid var(--line); border-radius:var(--radius);
    padding:28px; position:relative; overflow:hidden; transition:border-color .15s;
  }
  .ticket:hover{border-color:var(--line-strong);}
  .ticket::after{
    content:""; position:absolute; left:0; right:0; bottom:56px; height:0;
    border-bottom:2px dashed var(--line-strong);
  }
  .ticket .tag{
    font-family:'JetBrains Mono',monospace; font-size:11.5px; font-weight:700; letter-spacing:0.1em; text-transform:uppercase;
    padding:5px 12px; border-radius:20px; display:inline-block; margin-bottom:16px; border:2px solid currentColor;
  }
  .tag.amber{color:var(--amber);}
  .tag.cyan{color:var(--cyan);}
  .ticket h3{font-size:21px; font-weight:700; margin-bottom:5px;}
  .ticket .org{color:var(--slate); font-size:13.5px; font-weight:600; margin-bottom:4px;}
  .ticket .dates{font-family:'JetBrains Mono',monospace; font-weight:700; color:var(--slate); font-size:12px; margin-bottom:18px;}
  .ticket ul{margin:0; padding-left:18px; color:#cdd3de; font-size:14.5px;}
  .ticket ul li{margin-bottom:8px;}
  .ticket .stub{
    margin-top:20px; padding-top:16px; display:flex; justify-content:space-between;
    font-family:'JetBrains Mono',monospace; font-size:11px; font-weight:700; color:var(--slate); letter-spacing:0.08em;
  }

  /* project */
  .project-card{
    background:linear-gradient(150deg, var(--panel), var(--panel-2));
    border:2px solid var(--line-strong); border-radius:var(--radius); padding:36px;
    position:relative;
  }
  .route{
    display:flex; align-items:center; gap:10px; margin-bottom:20px;
    font-family:'JetBrains Mono',monospace; font-weight:700; font-size:12.5px; letter-spacing:0.08em; color:var(--slate);
  }
  .route .line{flex:1; height:2px; background:repeating-linear-gradient(90deg, var(--amber) 0 6px, transparent 6px 12px);}
  .project-card h3{font-size:clamp(24px,3.5vw,32px); font-weight:700; margin-bottom:12px;}
  .project-card p{color:#cdd3de; font-size:16px; font-weight:500; max-width:680px;}
  .project-list{margin:18px 0 24px; padding-left:18px; color:#cdd3de; font-size:14.5px;}
  .project-list li{margin-bottom:8px;}
  .stack{display:flex; flex-wrap:wrap; gap:10px; margin-bottom:26px;}
  .chip{
    font-family:'JetBrains Mono',monospace; font-size:12.5px; font-weight:700; padding:7px 14px; border-radius:20px;
    border:2px solid var(--line-strong); color:var(--paper);
  }
  .project-actions{display:flex; gap:12px; flex-wrap:wrap;}

  /* skills terminal */
  .terminal{
    background:var(--term); border:2px solid var(--line-strong); border-radius:var(--radius);
    overflow:hidden; max-width:720px;
    box-shadow:0 30px 60px -30px rgba(0,0,0,0.7);
  }
  .terminal-bar{
    display:flex; align-items:center; gap:8px; padding:12px 16px; background:#0d1a17; border-bottom:2px solid var(--line);
  }
  .terminal-bar span{width:10px;height:10px;border-radius:50%; display:inline-block; background:#3a3a3a;}
  .terminal-bar .t-title{
    margin-left:8px; font-family:'JetBrains Mono',monospace; font-weight:700; font-size:11.5px; color:var(--slate);
  }
  .terminal-body{padding:24px 20px; font-family:'JetBrains Mono',monospace; font-weight:500; font-size:14px; color:var(--cyan);}
  .terminal-body .prompt{color:var(--amber);}
  .terminal-body .path{color:var(--cyan);}
  .terminal-body .comment{color:var(--slate);}
  .terminal-body p{margin:0 0 6px;}
  .terminal-body .out{color:#dfeeec; padding-left:0; margin-bottom:16px;}
  .cursor{display:inline-block; width:8px; height:15px; background:var(--cyan); margin-left:4px; animation:blink 1s step-end infinite; vertical-align:-2px;}
  @keyframes blink{50%{opacity:0;}}
  @media (prefers-reduced-motion: reduce){ .cursor{animation:none;} }

  /* education + extra */
  .two-col{display:grid; grid-template-columns:1fr 1fr; gap:22px;}
  .plain-card{background:var(--panel); border:2px solid var(--line); border-radius:var(--radius); padding:28px;}
  .plain-card h3{font-size:19px; font-weight:700; margin-bottom:6px;}
  .plain-card .org{color:var(--slate); font-size:13.5px; font-weight:600; margin-bottom:4px;}
  .plain-card .dates{font-family:'JetBrains Mono',monospace; font-weight:700; color:var(--amber); font-size:12px;}
  .id-card{
    background:var(--panel); border:2px solid var(--line); border-radius:var(--radius); padding:28px;
  }
  .id-card ul{margin:12px 0 0; padding-left:18px; color:#cdd3de; font-size:14px;}
  .id-card ul li{margin-bottom:8px;}
  .id-card .badge{
    display:inline-flex; align-items:center; gap:8px; font-family:'JetBrains Mono',monospace; font-size:11.5px; font-weight:700;
    letter-spacing:0.08em; color:var(--cyan); border:2px solid var(--cyan); padding:6px 12px; border-radius:20px; margin-bottom:14px;
  }

  /* contact / footer */
  #contact{padding-top:60px;}
  .contact-panel{
    background:linear-gradient(150deg, var(--panel), var(--panel-2)); border:2px solid var(--line-strong);
    border-radius:var(--radius); padding:44px; display:flex; justify-content:space-between; gap:30px; flex-wrap:wrap;
  }
  .contact-panel h2{font-size:clamp(24px,4.5vw,34px); text-transform:none; max-width:440px;}
  .status-pill{
    font-family:'JetBrains Mono',monospace; font-size:12.5px; font-weight:700; letter-spacing:0.1em; color:var(--amber);
    border:2px solid var(--amber); padding:7px 14px; border-radius:20px; display:inline-flex; align-items:center; gap:8px; margin-bottom:20px;
  }
  .status-pill .dot{width:7px;height:7px;border-radius:50%;background:var(--amber); animation:pulse 2.2s infinite;}
  .contact-list{display:flex; flex-direction:column; gap:12px; font-family:'JetBrains Mono',monospace; font-weight:700; font-size:14px;}
  .contact-list a{text-decoration:none; display:flex; align-items:center; gap:10px; color:var(--paper);}
  .contact-list a:hover{color:var(--cyan);}
  .contact-list svg{flex:none;}
  footer.site-footer{
    padding:30px 0 50px; text-align:center; color:var(--slate); font-family:'JetBrains Mono',monospace; font-weight:700; font-size:12px;
  }

  @media (max-width: 760px){
    nav.links{display:none;}
    #menuToggle{display:inline-flex;}
    .board-row{grid-template-columns:1fr; gap:6px; row-gap:4px;}
    .board-row .status{justify-self:start;}
    .ticket-grid{grid-template-columns:1fr;}
    .two-col{grid-template-columns:1fr;}
    .contact-panel{flex-direction:column;}
    .navbar .btn span.long{display:none;}
  }
  nav.links.open{
    display:flex; position:absolute; top:100%; left:0; right:0; background:var(--panel);
    flex-direction:column; padding:16px 24px; border-bottom:1px solid var(--line); gap:16px;
  }
</style>
</head>
<body>

<header>
  <div class="navbar">
    <a href="#home" class="logo"><span class="logo-box">SD</span> SOUMYAJIT DAS</a>
    <nav class="links" id="navLinks">
      <a href="#work">Work</a>
      <a href="#projects">Projects</a>
      <a href="#skills">Skills</a>
      <a href="#education">Education</a>
      <a href="#contact">Contact</a>
    </nav>
    <div class="actions">
      <a class="btn btn-amber" href="resume.pdf" download><span class="long">↓</span> Resume</a>
      <button id="menuToggle" aria-label="Toggle menu" aria-expanded="false">☰</button>
    </div>
  </div>
</header>

<main>

  <section class="hero" id="home">
    <div class="wrap">
      <div class="eyebrow">Departures // Career Board</div>
      <div class="board">
        <div class="board-head"><span><span class="dot"></span>LIVE — Diamond Harbour, West Bengal, India</span><span>Local time board</span></div>
        <div class="board-row">
          <span class="code">SD-01</span>
          <span class="role">AI/ML Engineering Intern</span>
          <span class="org">Euphoria GenX · New Town</span>
          <span class="status ontime">Jun–Jul 2026</span>
        </div>
        <div class="board-row">
          <span class="code">SD-02</span>
          <span class="role">Cybersecurity Intern</span>
          <span class="org">WEBEL · New Town</span>
          <span class="status landed">2025–2026</span>
        </div>
        <div class="board-row">
          <span class="code">SD-03</span>
          <span class="role">BCA, Computer Applications</span>
          <span class="org">The Neotia University &amp; NITMAS</span>
          <span class="status board">2024–2028</span>
        </div>
      </div>

      <div class="hero-name">
        <h1 class="flap-line js-flap" data-text="SOUMYAJIT DAS"></h1>
        <p class="tagline">Building <span class="hl">intelligent systems</span> in Python — and learning to keep them <span class="hl2">secure</span>.</p>
        <p class="hero-copy">BCA student and dual-track intern working across AI/ML and cybersecurity — from training flight-price prediction models to hardening Linux systems. Looking for internships and entry-level roles where I can keep doing both.</p>
        <div class="hero-ctas">
          <a class="btn btn-lg btn-amber" href="resume.pdf" download>Download résumé</a>
          <a class="btn btn-lg btn-ghost" href="https://github.com/Zen-Zero10" target="_blank" rel="noopener">View GitHub ↗</a>
          <a class="btn btn-lg btn-ghost" href="mailto:soumyajit.das24@tnu.in">Email me</a>
        </div>
      </div>
    </div>
  </section>

  <hr class="divider">

  <section id="about">
    <div class="wrap">
      <div class="eyebrow">About</div>
      <p><strong>I'm a BCA student in Diamond Harbour, West Bengal</strong>, currently reading Computer Applications at The Neotia University &amp; NITMAS (2024–2028). My work sits at the intersection of two things I care about: getting machine learning models to make useful predictions, and understanding how systems get compromised so they can be defended. I've interned on both sides — building a flight price prediction pipeline in Python, and learning Linux administration and ethical hacking fundamentals at a state IT corporation. I'm also an NCC 'A' certificate holder, which is where the discipline and teamwork habits come from.</p>
    </div>
  </section>

  <hr class="divider">

  <section id="work">
    <div class="wrap">
      <div class="eyebrow">Work Log</div>
      <h2>Internships</h2>
      <p class="section-sub">Two tracks, run in parallel: applied AI/ML, and cybersecurity fundamentals.</p>
      <div class="ticket-grid">
        <div class="ticket">
          <span class="tag amber">AI / ML</span>
          <h3>AI &amp; Machine Learning using Python</h3>
          <div class="org">Euphoria GenX, New Town</div>
          <div class="dates">JUN 2026 — JUL 2026</div>
          <ul>
            <li>Completed the Euphoria GenX Summer Internship 2026 in AI and Machine Learning using Python.</li>
            <li>Built a flight price prediction project with a collaborative team, covering the full ML workflow.</li>
            <li>Hands-on with data preprocessing, feature engineering, model training and prediction.</li>
            <li>Developed teamwork and project-delivery skills under mentorship on a real-world AI/ML application.</li>
          </ul>
          <div class="stub"><span>BOARDING GROUP: INTERN</span><span>SEAT: NEW TOWN</span></div>
        </div>
        <div class="ticket">
          <span class="tag cyan">CYBERSECURITY</span>
          <h3>Cybersecurity Internship</h3>
          <div class="org">West Bengal Electronics Industry Development Corporation (WEBEL), New Town</div>
          <div class="dates">JUN 2025 — JUL 2026</div>
          <ul>
            <li>Completed a summer internship at WEBEL focused on Linux administration and cybersecurity.</li>
            <li>Learned Linux fundamentals — command line, file management, user administration, shell scripting.</li>
            <li>Gained hands-on exposure to core security concepts and ethical hacking fundamentals.</li>
            <li>Built troubleshooting and problem-solving skills through guided lab sessions.</li>
          </ul>
          <div class="stub"><span>BOARDING GROUP: INTERN</span><span>SEAT: NEW TOWN</span></div>
        </div>
      </div>
    </div>
  </section>

  <hr class="divider">

  <section id="projects">
    <div class="wrap">
      <div class="eyebrow">Featured Build</div>
      <h2>Flight Price Prediction</h2>
      <p class="section-sub">The flagship project from the Euphoria GenX internship — an end-to-end ML pipeline, not just a notebook.</p>
      <div class="project-card">
        <div class="route"><span>DATA</span><span class="line"></span><span>MODEL</span><span class="line"></span><span>APP</span></div>
        <h3>Predicting flight ticket prices with ML</h3>
        <p>A machine learning system that predicts flight ticket prices from historical and real-world flight data, built as part of a collaborative team during the Euphoria GenX Summer Internship, mentored by Techno Exponent.</p>
        <ul class="project-list">
          <li>Ran data preprocessing and exploratory data analysis to understand pricing patterns.</li>
          <li>Engineered features and trained prediction models on real flight data.</li>
          <li>Evaluated model performance and iterated on accuracy.</li>
          <li>Deployed the working application with Streamlit for interactive use.</li>
        </ul>
        <div class="stack">
          <span class="chip">Python</span>
          <span class="chip">Pandas</span>
          <span class="chip">NumPy</span>
          <span class="chip">Scikit-learn</span>
          <span class="chip">Matplotlib</span>
          <span class="chip">Streamlit</span>
        </div>
        <div class="project-actions">
          <a class="btn btn-amber" href="https://github.com/Zen-Zero10" target="_blank" rel="noopener">View on GitHub ↗</a>
        </div>
      </div>
    </div>
  </section>

  <hr class="divider">

  <section id="skills">
    <div class="wrap">
      <div class="eyebrow cy">Skills</div>
      <h2>What I work with</h2>
      <p class="section-sub">Rendered the way I'd actually check it — from a terminal.</p>
      <div class="terminal">
        <div class="terminal-bar">
          <span style="background:#ff5f56"></span><span style="background:#ffbd2e"></span><span style="background:#27c93f"></span>
          <span class="t-title">soumyajit@career — zsh</span>
        </div>
        <div class="terminal-body">
          <p><span class="prompt">$</span> whoami</p>
          <p class="out">soumyajit_das — bca student · ai/ml + cybersecurity</p>
          <p><span class="prompt">$</span> ls languages/</p>
          <p class="out">python&nbsp;&nbsp;&nbsp;java&nbsp;&nbsp;&nbsp;c</p>
          <p><span class="prompt">$</span> ls web/</p>
          <p class="out">html&nbsp;&nbsp;&nbsp;css</p>
          <p><span class="prompt">$</span> ls security/</p>
          <p class="out">linux-administration&nbsp;&nbsp;&nbsp;ethical-hacking-fundamentals&nbsp;&nbsp;&nbsp;system-security</p>
          <p><span class="prompt">$</span> <span class="comment"># currently learning: ML deployment, applied cybersecurity</span></p>
          <p><span class="prompt">$</span> <span class="cursor"></span></p>
        </div>
      </div>
    </div>
  </section>

  <hr class="divider">

  <section id="education">
    <div class="wrap">
      <div class="eyebrow">Education &amp; Service</div>
      <div class="two-col">
        <div class="plain-card">
          <h3>Bachelor of Computer Applications (BCA)</h3>
          <div class="org">The Neotia University &amp; NITMAS</div>
          <div class="dates">2024 — 2028</div>
        </div>
        <div class="id-card">
          <span class="badge">NCC 'A' CERTIFICATE</span>
          <h3>43 Bengal Battalion, NCC</h3>
          <ul>
            <li>NCC 'A' Certificate holder.</li>
            <li>Republic Day Parade Drill participant, representing schools in Chandannagar.</li>
            <li>Member of the Best Squad of 2024 for discipline and coordination.</li>
            <li>Built leadership, teamwork, discipline and communication skills through NCC training.</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <hr class="divider">

  <section id="contact">
    <div class="wrap">
      <div class="contact-panel">
        <div>
          <span class="status-pill"><span class="dot"></span>OPEN TO OPPORTUNITIES</span>
          <h2>Looking for an AI/ML or cybersecurity internship? Let's talk.</h2>
        </div>
        <div class="contact-list">
          <a href="mailto:soumyajit.das24@tnu.in">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 4h16v16H4z"/><path d="M4 4l8 8 8-8"/></svg>
            soumyajit.das24@tnu.in
          </a>
          <a href="tel:+917439479770">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 5c0 9 7 16 16 16l3-5-6-2-2 2c-3-1-5-3-6-6l2-2-2-6z"/></svg>
            +91 74394 79770
          </a>
          <a href="https://github.com/Zen-Zero10" target="_blank" rel="noopener">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M8 21c1-3 1-6 0-9M16 21c-1-3-1-6 0-9"/></svg>
            github.com/Zen-Zero10
          </a>
          <a href="#home">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v6M5 9l7-6 7 6M5 9v11a1 1 0 001 1h4v-6h4v6h4a1 1 0 001-1V9"/></svg>
            Diamond Harbour, West Bengal, India
          </a>
        </div>
      </div>
    </div>
  </section>

</main>

<footer class="site-footer">
  <div class="wrap">SD // Open To Work</div>
</footer>

<script>
  // Mobile menu
  const menuToggle = document.getElementById('menuToggle');
  const navLinks = document.getElementById('navLinks');
  menuToggle.addEventListener('click', () => {
    const isOpen = navLinks.classList.toggle('open');
    menuToggle.setAttribute('aria-expanded', isOpen);
  });
  navLinks.querySelectorAll('a').forEach(a => a.addEventListener('click', () => navLinks.classList.remove('open')));

  // Split-flap text effect
  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  const CHARSET = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";

  function flap(el){
    const text = el.dataset.text || el.textContent;
    el.textContent = '';
    if (reduceMotion){ el.textContent = text; return; }

    [...text].forEach((ch, i) => {
      const cell = document.createElement('span');
      cell.className = 'flap-cell';
      cell.textContent = ch === ' ' ? '\u00A0' : ch;
      el.appendChild(cell);

      const ticks = 7 + Math.floor(Math.random() * 5);
      let count = 0;
      const startDelay = i * 35;

      function tick(){
        cell.classList.remove('flip');
        void cell.offsetWidth;
        cell.classList.add('flip');
        const isLast = count === ticks - 1;
        const shown = isLast ? (ch === ' ' ? '\u00A0' : ch) : CHARSET[Math.floor(Math.random() * CHARSET.length)];
        setTimeout(() => { cell.textContent = shown; }, 60);
        count++;
        if (count < ticks) setTimeout(tick, 85);
      }
      setTimeout(tick, startDelay);
    });
  }

  document.querySelectorAll('.js-flap').forEach(el => flap(el));
</script>

</body>
</html>
